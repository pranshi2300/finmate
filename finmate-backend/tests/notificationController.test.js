const assert = require('assert');
const servicePath = require.resolve('../src/services/notifications/notificationService');
require.cache[servicePath] = { id: servicePath, filename: servicePath, loaded: true, exports: { listNotifications: async () => ({ notifications: [], unreadCount: 2 }), markRead: async () => true, markAllRead: async () => ({ count: 2 }), deleteNotification: async () => true, createTestNotification: async () => ({ id: 'test' }), getPreferences: async () => ({ enabled: true }), updatePreferences: async (_id, values) => values } };
const controller = require('../src/controllers/notificationController');
function response() { return { statusCode: 200, body: null, status(code) { this.statusCode = code; return this; }, json(value) { this.body = value; return this; }, send() { return this; } }; }
(async () => { const res = response(); await controller.unread({ user: { id: 'u1' }, query: {} }, res); assert.strictEqual(res.body.unreadCount, 2); const pref = response(); await controller.updatePreferences({ user: { id: 'u1' }, body: { budget: false } }, pref); assert.strictEqual(pref.body.preferences.budget, false); const read = response(); await controller.markRead({ user: { id: 'u1' }, params: { id: 'n1' } }, read); assert.strictEqual(read.statusCode, 204); console.log('notification controller tests passed'); })().catch((error) => { console.error(error); process.exitCode = 1; });
