const COLORS = { critical: 'border-signal bg-signal/15', high: 'border-signal/60 bg-signal/10', medium: 'border-hairline bg-ink/80', low: 'border-hairline bg-white/20' };
export default function NotificationCard({ notification, onRead, onDelete }) {
  return <article className={`rounded-xl border p-3 ${COLORS[notification.priority] || COLORS.low} ${notification.read ? 'opacity-70' : ''}`}><div className="flex items-start justify-between gap-3"><div><p className="font-display text-sm text-bone">{notification.title}</p><p className="font-body text-xs text-bone/75 mt-1">{notification.description}</p><p className="font-mono text-[10px] text-bone/60 mt-2">{notification.priority} · {new Date(notification.createdAt).toLocaleDateString('en-IN')}</p></div><div className="flex gap-2 shrink-0">{!notification.read && <button onClick={() => onRead(notification.id)} className="font-mono text-[10px] text-ledger hover:underline">Read</button>}<button onClick={() => onDelete(notification.id)} className="font-mono text-[10px] text-signal hover:underline">Delete</button></div></div></article>;
}
