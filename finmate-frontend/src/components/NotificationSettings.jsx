const FIELDS = [['budget', 'Budget'], ['forecasts', 'Forecasts'], ['subscriptions', 'Subscriptions'], ['savings', 'Savings'], ['aiAdvisor', 'AI Advisor'], ['achievements', 'Achievements']];
export default function NotificationSettings({ preferences, onChange }) { if (!preferences) return null; return <section className="border border-hairline rounded-xl p-4 card-surface"><p className="font-mono text-xs text-bone/65 uppercase mb-3">Notification settings</p><label className="flex justify-between gap-3 font-body text-sm text-bone mb-3">Enable notifications<input type="checkbox" checked={preferences.enabled} onChange={(event) => onChange('enabled', event.target.checked)} /></label><div className="grid grid-cols-2 gap-2">{FIELDS.map(([field, label]) => <label key={field} className="flex justify-between gap-2 font-body text-xs text-bone/75">{label}<input type="checkbox" checked={preferences[field]} disabled={!preferences.enabled} onChange={(event) => onChange(field, event.target.checked)} /></label>)}</div></section>; }
